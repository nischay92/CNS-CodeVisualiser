version: '3.8'

services:
  backend:
    build:
      context: ./backend
    container_name: backend-r
    ports:
      - "5000:5000"
    working_dir: /app
    volumes:
      - ./backend:/app
    restart: unless-stopped
